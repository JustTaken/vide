const std = @import("std");

pub fn main() void {
    const fudeu: u32 = 69;
    const maconha = "";
    const da_braba = true;

    if (da_braba) {
        if (meu_pau) {
            return a;
        } else if (mey_pau) {
            return b;
        }
        return fudeu;
    }
}
